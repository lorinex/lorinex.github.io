---
categories:
  - 编程语言
  - Java
  - 基础
---
# 运算符

## 逻辑运算符

#### &&和&基本规则

| 名称        | 语法           | 特点                         | 区别                                                         |
| ----------- | -------------- | ---------------------------- | ------------------------------------------------------------ |
| 短路与 `&&` | 条件1`&&`条件2 | 两个条件都为true，结果为true | 如果第一个条件为false，则第二个条件不会判断，最终结果为false |
| 逻辑与`&`   | 条件1`&`条件2  | 两个条件都为true，结果为true | 不管第一个条件是否为false，第二个条件都要判断                |

开发中，基本使用`&&`

#### `||` 和`|`基本规则

| 名称        | 语法           | 特点                                   | 区别                                                       |
| ----------- | -------------- | -------------------------------------- | ---------------------------------------------------------- |
| 短路或 `||` | 条件1`||`条件2 | 两个条件只要有一个条件成立，结果为true | 如果第一个条件为true，则第二个条件不会判断，最终结果为true |
| 逻辑或`|`   | 条件1`|`条件2  | 两个条件只要有一个条件成立，结果为true | 不管第一个条件是否为true，第二个条件都要判断               |

开发中，基本使用`||`

#### `!`非

#### `^` 异或

## 三元运算符

#### 基本语法

条件表达式？表达式1：表达式2;

运算规则：

1. 如果条件表达式为true，运算后的结果是表达式1；
2. 如果条件表达式为false，运算后的结果是表达式2；

```java
int a = 10;
int b = 99;
int result = a > b ? a++ : b--;
//result=99；b=98;
//a>b 为false
//返回b--,先返回b的值，然后再b-1
//返回的结果是99
```

使用细节：

1. 表达式1和表达式2要为可以赋给接收变量的类型（或可以自动转换）

2. 三元运算符可以转成if-else语句

   ```java
   int res=a>b?a++:--b;
   if(a>b) res=a++;
   else res=--b;
   ```


## 运算符的优先级

1. （），{}等
2. 单目 ++ --
3. 算术运算符
4. 位移运算符
5. 比较运算符
6. 逻辑运算符
7. 三元运算符
8. 赋值运算符
