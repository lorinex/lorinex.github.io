---
categories:
  - 编程语言
  - Java
  - 基础
---
# 原码 补码 反码

整数的原码、补码、反码都一样

负数的反码=它的原码符号不变，其他位取反`(0->1 1->0)`

负数的补码=它的反码+1

0的反码、补码都是0

java没有无符号数，java中的数都是有符号的

计算机在运算的时候，都是以**补码的方式来运算**的

当我们看运算结果时，要看他的原码

### 位运算符

按位与`&` 按位或`|` 按位异或`^` 按位取反`~`

`>>`算术右移

`<<`算术左移

`>>>`逻辑右移、无符号右移，没有`<<<`符号

